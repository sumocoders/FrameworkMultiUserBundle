{% extends '::empty.html.twig' %}

{% block main %}

  {{ form_start(form) }}
  {{ form_rest(form) }}
  <input type="submit" class="btn btn-default" value="{{ 'sumocoders.multiuserbundle.form.submit'|trans|capitalize }}">
  {{ form_end(form) }}

{% endblock %}
